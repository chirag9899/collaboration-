import styled from "styled-components";

export const SectionTitle = styled.span`
  display: flex;
  flex-direction: column;
  gap: 8px;
  font-style: normal;
  font-weight: 600;
  font-size: 16px;
  line-height: 24px;
  color: var(--neutral-1);
  margin-bottom: 16px;
`;
